import CoinCarousel from '../../../carousels/coinCarousel/CoinCarousel'

function FooterPanel() {
  return (
    <div className={`text-white h-fit pb-1 overflow-hidden `}>
      <CoinCarousel />
    </div>
  )
}

export default FooterPanel

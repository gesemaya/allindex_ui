import logo from '../../assets/logo-gradient-bg.svg'

export const WhitelistLogo = () => {
  return (
    <div className={'mx-auto w-max h-[112px]'}>
      <img src={logo} alt={''} className={'w-[64px]'} />
    </div>
  )
}

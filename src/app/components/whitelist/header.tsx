export const WhitelistHeaders = ({ children }: { children: React.ReactNode }) => {
  return <div className={'h-[156px]'}>{children}</div>
}

export const WhitelistH1 = ({ children }: { children: React.ReactNode }) => {
  return <h1 className={'text-center font-medium text-[#F5F6FC] text-[24px] p-[4px]'}>{children}</h1>
}

export const WhitelistH2 = ({ children }: { children: React.ReactNode }) => {
  return <h2 className={'text-center text-[#99A1BD] text-[16px] p-[2px]'}>{children}</h2>
}
